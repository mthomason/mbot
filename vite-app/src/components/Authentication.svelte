
<!--
<script>
	import { onMount } from 'svelte';
	import { initializeApp, getApps, } from 'firebase/app';

	onMount(() => {
		if (!getApps().length) {
			initializeApp({ // Firebase configurations
		
			});
		}
	});
</script>
*/
-->
